export type ResCartLine = { localId: string; service_id: number };

export type DailyAgendaPageProps = {
  /** Dentro del hub Caja & seguimiento: sin título global duplicado. */
  embedded?: boolean;
};
