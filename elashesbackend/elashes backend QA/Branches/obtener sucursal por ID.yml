info:
  name: obtener sucursal por ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/branches/{{branchId}}"
  headers:
    - name: Authorization
      value: "{{token}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
