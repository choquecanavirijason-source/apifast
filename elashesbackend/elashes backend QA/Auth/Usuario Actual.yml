info:
  name: Usuario Actual
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/auth/me"
  headers:
    - name: Authorization
      value: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
