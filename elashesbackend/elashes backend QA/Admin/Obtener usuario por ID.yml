info:
  name: Obtener usuario por ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/admin/users/{{userId}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
