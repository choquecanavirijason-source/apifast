info:
  name: Crear permiso nuevo
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/admin/permissions"
  body:
    type: json
    data: |-
      {
        "name" : "report:view"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
