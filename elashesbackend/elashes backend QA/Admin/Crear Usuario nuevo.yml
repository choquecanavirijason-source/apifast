info:
  name: Crear Usuario nuevo
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/admin/users"
  body:
    type: json
    data: |-
      {
        "username": "recepcion1",
        "email": "recepcion1@elashes.com",
        "password": "recepcion123",
        "is_active": true,
        "role_id": 3,
        "branch_id": 1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
