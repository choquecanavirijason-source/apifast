info:
  name: Obtener cliente por ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/clients/4"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
