info:
  name: Actualizar clienta
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/clients/4"
  body:
    type: json
    data: |-
      {
        "age" : 28,
        "phone" : "70000323"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
