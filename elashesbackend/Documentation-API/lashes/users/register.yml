info:
  name: register
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8000/auth/register
  body:
    type: json
    data: |-
      {
        "email": "admin1@gmail.com",
        "password":"passadmin",
        "full_name": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
